#!/bin/sh
. /lib/functions.sh

ROOTFS_DATA_DEV=$(find_mtd_part rootfs_data)
echo "PASSED: set default ok, the device will reboot" > /dev/pts/0
dd if=/dev/zero of="$ROOTFS_DATA_DEV" bs=512k count=1
sleep 1
reboot